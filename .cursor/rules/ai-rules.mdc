---
alwaysApply: true
---

# Node API 测试服务器 - AI 主规则

> **本文件用途**：定义项目的核心规则、工作流程、规则文件索引。
> 
> **alwaysApply: true** - Cursor 每次都会读取此文件。

---

## 📚 规则文件索引

本项目将规则分为多个专项文件，AI 应根据当前任务类型选择性读取：

### 1. **API 接口生成规则** (`.cursor/rules/api-generation.mdc`)
- **用途**：生成 API 接口模块的完整规范
- **触发时机**：用户提供接口文档、需求文档，要求生成接口
- **内容**：文件结构、代码模板、响应格式、常见错误修复
- **alwaysApply**: `false`（AI 根据需要读取）

### 2. **UI/UX 设计规范** (`.cursor/rules/ui-guidelines.mdc`)
- **用途**：前端页面的设计规范和样式标准
- **触发时机**：开发/修改后台管理页面、创建组件、调整样式
- **内容**：页面布局、颜色系统、按钮规范、表格样式、模态框规范
- **alwaysApply**: `false`（AI 根据需要读取）

### 3. **后台管理系统上下文** (`.cursor/rules/backend-admin-context.mdc`)
- **用途**：后台管理系统的功能、页面、组件说明
- **触发时机**：修改后台功能、添加新页面、调试后台问题
- **内容**：页面详解、组件说明、API 列表、路由管理
- **alwaysApply**: `false`（AI 根据需要读取）

### 4. **项目整体上下文** (`.cursor/rules/project-context.mdc`)
- **用途**：项目整体架构、目录结构、核心模块说明
- **触发时机**：架构调整、添加新模块、需要理解项目全貌
- **内容**：目录结构、核心模块说明、数据流、技术栈
- **alwaysApply**: `false`（AI 根据需要读取）

---

## 🔄 规则自动收集机制（元规则）

### 工作原理
在与用户的对话过程中，AI 需要主动识别和收集新的规则：

1. **识别规则时机**：
   - 用户明确提到"规则"、"不要"、"必须"、"禁止"等关键词
   - 用户纠正 AI 的行为
   - 用户表达对某种做法的偏好
   - 用户说"记住"、"以后"、"下次"等表示未来行为的词

2. **判断是否收集**：
   - ✅ 适用于整个项目的通用规则
   - ✅ 会重复发生的行为规范
   - ✅ AI 容易犯的错误或误解
   - ❌ 一次性的具体任务指令
   - ❌ 特定模块的业务逻辑（应记录在模块的 `.prompt.md` 中）

3. **自动更新规则文件**：
   - 识别到新规则后，判断应该添加到哪个规则文件
   - **API 生成相关** → 更新 `api-generation.mdc`
   - **UI/UX 相关** → 更新 `ui-guidelines.mdc`
   - **后台管理相关** → 更新 `backend-admin-context.mdc`
   - **项目架构相关** → 更新 `project-context.mdc`
   - **通用规则** → 更新本文件 (`ai-rules.mdc`)
   - 规则应该清晰、简洁、可执行
   - 使用清单格式（✅/❌）便于快速扫描

4. **规则分类示例**：
   - **文件生成规则** → 本文件
   - **API 接口生成** → `api-generation.mdc`
   - **UI/UX 设计** → `ui-guidelines.mdc`
   - **后台页面功能** → `backend-admin-context.mdc`
   - **项目目录结构** → `project-context.mdc`
   - **临时文件管理** → 本文件
   - **模板同步机制** → `api-generation.mdc`

5. **如果规则不属于现有任何文件**：
   - 判断是否有必要创建新的规则文件
   - 如果是独立的、重要的主题（如"测试规范"、"部署规范"），创建新文件
   - 在本文件的"规则文件索引"章节添加新文件的说明
   - 新文件必须包含：
     - `alwaysApply: false` 声明
     - 文件用途说明
     - 触发时机说明
     - 自动收集机制章节

### 收集示例
**示例 1**：
- 用户说："不要生成无意义的文档"
- AI 识别：这是通用的文件生成规则
- AI 收集：添加到本文件的"文件生成规则"部分
- 格式化为：`❌ 禁止生成任何总结类文档`

**示例 2**：
- 用户说："model.ts 中的 LIMIT 必须直接拼接，不能用占位符"
- AI 识别：这是 API 生成的技术细节
- AI 收集：添加到 `api-generation.mdc` 的"常见错误与修复"部分

**示例 3**：
- 用户说："按钮高度统一为 32px"
- AI 识别：这是 UI 设计规范
- AI 收集：添加到 `ui-guidelines.mdc` 的"按钮规范"部分

**示例 4**：
- 用户说："添加一个模块编辑功能到后台"
- AI 识别：这是后台管理功能变更
- AI 收集：添加到 `backend-admin-context.mdc` 的对应章节

**示例 5**：
- 用户说："添加 Redis 缓存模块"
- AI 识别：这是项目架构变更
- AI 收集：添加到 `project-context.mdc` 的"核心模块说明"章节

**示例 6（新规则文件）**：
- 用户说："所有 API 必须有单元测试，覆盖率不低于 80%"
- AI 识别：这是测试规范，不属于现有任何文件
- AI 判断：这是重要的独立主题
- AI 操作：
  1. 创建 `.cursor/rules/testing-guidelines.mdc`
  2. 在本文件添加索引
  3. 将规则写入新文件

---

## 🎯 基本规则

### 项目定位
- ✅ 这是一个 **AI 驱动的 Mock API 测试服务器**
- ✅ 用户通过 Cursor 提供接口文档/需求文档，AI 生成可运行的接口
- ✅ 生成的接口有真实的数据库支持，不是简单的 JSON 文件
- ✅ 提供后台管理页面，方便调试和测试

### 核心工作流程
1. 用户提供接口文档/需求文档
2. AI 根据 `api-generation.mdc` 规则生成模块代码
3. 用户运行 `init-db.ts` 初始化数据库
4. 用户在后台管理页面查看、测试、管理接口

### 文件结构
```
项目根目录/
├── src/
│   ├── core/           # 核心功能（BaseModel、Response、ErrorHandler）
│   ├── config/         # 配置（数据库、CORS、上传）
│   ├── middlewares/    # 中间件（日志、延迟、错误模拟、接口控制）
│   ├── utils/          # 工具（Faker、Logger）
│   ├── system/         # 系统接口（后台管理 API）
│   └── generated/      # AI 生成的接口模块
│       └── [模块名]/
│           ├── .prompt.md          # 【必需】模块级提示词
│           ├── module.config.ts    # 【必需】模块配置
│           ├── types.ts            # 【必需】类型定义
│           ├── model.ts            # 【必需】数据模型
│           ├── controller.ts       # 【必需】控制器
│           ├── routes.ts           # 【必需】路由
│           ├── schema.sql          # 【必需】数据库表结构
│           └── init-db.ts          # 【可选】初始化脚本
├── public/
│   └── admin/          # 后台管理页面
│       ├── pages/      # 页面（dashboard、data、interfaces、scenarios、test、help）
│       ├── components/ # 组件（modal、toast、table、form）
│       └── index.html  # 入口文件
└── .cursor/
    └── rules/          # AI 规则文件
        ├── ai-rules.mdc        # 【主规则，本文件】
        ├── api-generation.mdc  # API 生成规则
        └── ui-guidelines.mdc   # UI 设计规范
```

### 模块级提示词机制
- ✅ **每个生成的模块必须包含 `.prompt.md` 文件**
- ✅ 位置：`src/generated/[模块名]/.prompt.md`
- ✅ 用途：记录该模块的业务逻辑、接口说明、特殊处理、已知问题
- ✅ 模块的特殊调整应记录在模块级 `.prompt.md`
- ✅ 通用规则应更新到 `.cursor/rules/` 中的对应规则文件

### 基本原则
- ✅ 使用中文与用户沟通
- ✅ 接口生成到 `src/generated/[模块名]/`
- ✅ 每个模块**必须包含** `.prompt.md` 文件
- ✅ 后台管理功能不能受影响（最高优先级）

---

## 📝 文档识别规则（重要！）

### 当用户提供文档时
- ✅ **默认理解为要求根据文档生成接口**
- ✅ **不要询问"你给我文档是要做什么？"**
- ✅ **直接开始分析文档并生成代码**
- ✅ **文档可能包含**：API 接口文档、需求说明、数据库表结构等
- ✅ **开始前简单复述理解到的需求即可，无需过多提问**

### 生成流程
1. 用户提供接口文档/需求文档
2. AI 读取 `.cursor/rules/api-generation.mdc` 获取详细规范
3. 按照规范生成所有必需文件
4. 提示用户运行 `init-db.ts` 初始化数据库
5. 提示用户访问后台管理页面验证功能

---

## 📄 文件生成规则（重要！）

### 禁止生成的文件
- ❌ **总结类文档**（如 PROJECT_SUMMARY.md、GETTING_STARTED.md、CHANGELOG.md）
- ❌ **无意义文档**（如 FIX_REPORT.md、TODO.md）
- ❌ **项目总结、进度报告**（在聊天中说明即可）

### 允许生成的文件
- ✅ `README.md`（项目说明，必需）
- ✅ 模块级 `.prompt.md`（每个模块必需）
- ✅ API 模块的必需文件（见 `api-generation.mdc`）

---

## 🗑️ 临时文件管理规则（重要！）

### 临时文件定义
临时文件包括但不限于：
- 数据库修复脚本（如 `fix-table.ts`、`recreate-table.ts`）
- 测试数据插入脚本（如 `insert-data.ts`）
- 调试用脚本
- 实验性代码文件

### 管理规则
- ❌ **禁止保留调试/测试用的临时脚本文件**
- ✅ **完成任务后必须清理所有临时生成的文件**
- ✅ **删除源文件（.ts）和编译后的文件（.js）**
- ✅ 永久性的初始化脚本应该放在 `scripts/` 目录
- ✅ 模块的初始化脚本应命名为 `init-db.ts`（用于首次创建表和初始数据）

---

## 📖 使用说明页面维护规则（重要！）

### 何时更新
当项目使用方式有任何变更时，必须同步更新使用说明页面。

### 需要更新的情况
- 新增功能或页面
- 修改现有功能的使用方式
- 删除功能
- 修改配置方式
- 修改项目启动流程
- 修改模块开发流程
- 新增或修改 API 路由规则

### 更新要求
- ✅ 使用说明页面位置：`public/admin/pages/help.js`
- ✅ 更新时机：在完成功能变更后，立即更新使用说明页面
- ✅ 更新原则：保持说明清晰、准确、完整

---

## 🧪 需求分析规则（重要！）

### 适用范围
**本规则仅适用于"项目本身的功能改进需求"，不适用于"根据接口文档生成 API 模块"的需求**

- ✅ 适用：改进管理后台、修改项目结构、优化功能、添加工具等
- ❌ 不适用：用户提供接口文档要求生成 API 模块（直接执行即可）

### 分析流程
- ✅ **当用户提出项目功能改进需求时，AI 应先分析合理性，提出改进建议**
- ✅ **如有多种实现方案，应向用户说明各方案的优缺点**
- ✅ **征得用户确认后再执行实施**
- ✅ **对用户的想法保持开放态度，但要提供专业建议**
- ✅ 用户的想法可能具有片面性，AI 应该：
  - 分析需求的合理性
  - 识别潜在问题
  - 提出更优的实现方案
  - 与用户讨论确认后再实施
- ✅ 避免直接"照做"，而应该作为专业顾问提供建议
- ✅ 如果用户方案存在明显问题，必须指出并建议替代方案

### 示例说明
**需要分析的情况：**
- ❓ "我想把模块管理和接口管理合并成一个页面"
  → 需要分析：合并是否合理，是否有更好的方案
  
**不需要分析的情况：**
- ✓ "帮我生成一个用户管理模块，包含增删改查接口"
  → 直接根据接口文档生成即可，无需讨论方案

---

## 🎯 核心工具说明

### BaseModel（数据模型基类）
- 位置：`src/core/BaseModel.ts`
- 提供基础 CRUD 方法：`findAll`, `findById`, `create`, `update`, `delete`
- 详细使用方法见 `api-generation.mdc`

### Res（响应封装）
- 位置：`src/core/Response.ts`
- 支持三种响应格式：B2B、ERP、Ebooking
- 成功响应：`code: 0`（所有格式统一）
- 详细使用方法见 `api-generation.mdc`

### Faker（数据生成）
- 位置：`src/utils/faker.ts`
- 根据表结构自动生成测试数据
- 详细使用方法见 `api-generation.mdc`

---

## 📌 重要提醒

1. **后台管理优先**：
   - 生成的接口必须能在后台管理页面正常使用
   - 如果后台功能异常，说明生成的代码有问题
   - 详细说明见 `backend-admin-context.mdc`

2. **生成的模块可以修改**：
   - 在必要情况下，生成的模块是可以修改的
   - **修改前必须评估影响**：是否会影响后台管理功能
   - **修改后必须验证**：确保后台管理功能正常
   - **如有疑问必须确认**：如果可能出现功能异常，必须先与用户确认解决方案
   - **特殊调整记录在模块的 `.prompt.md`**：模块级的调整记录在模块内
   - **通用问题更新规则文件**：如果是通用问题，必须更新对应的规则文件

3. **规则文件分工**：
   - 本文件：通用规则、文件管理、工作流程
   - `api-generation.mdc`：API 生成的详细技术规范
   - `ui-guidelines.mdc`：前端页面的设计规范
   - `backend-admin-context.mdc`：后台管理系统说明
   - `project-context.mdc`：项目整体架构说明

4. **遇到问题时**：
   - API 生成问题 → 查看 `api-generation.mdc`
   - UI/样式问题 → 查看 `ui-guidelines.mdc`
   - 后台功能问题 → 查看 `backend-admin-context.mdc`
   - 项目架构问题 → 查看 `project-context.mdc`
   - 工作流程问题 → 查看本文件

5. **所有规则/上下文文件都有自动收集机制**：
   - 每个文件都包含"自动收集机制"章节
   - 发现新规则/上下文时，AI 应自动识别并更新对应文件
   - 本文件定义了总体的收集规则和分类标准

---

**最后提醒**：
- 本文件是所有规则的入口和索引
- 具体技术细节在专项规则文件中
- 发现新的通用规则时，立即更新本文件！
