---
alwaysApply: true
---

# Node API 测试服务器 - AI 主规则

> **用途**：定义项目的核心规则、工作流程、规则文件索引。
> 
> **alwaysApply: true** - Cursor 每次都会读取此文件。

---

## 📚 规则文件索引

本项目将规则分为多个专项文件，AI 应根据当前任务类型选择性读取：

### 1. **API 接口生成规则** (`.cursor/rules/api-generation.mdc`)
- **用途**：生成 API 接口模块的完整规范
- **触发时机**：用户要求生成接口（完整文档或简单需求如"生成订单管理"）
- **内容**：文件结构、代码模板、响应格式、常见错误修复

### 2. **UI/UX 设计规范** (`.cursor/rules/ui-guidelines.mdc`)
- **用途**：前端页面的设计规范和样式标准
- **触发时机**：开发/修改后台管理页面、创建组件、调整样式
- **内容**：页面布局、颜色系统、按钮规范、表格样式、模态框规范

### 3. **后台管理系统上下文** (`.cursor/rules/backend-admin-context.mdc`)
- **用途**：后台管理系统的功能、页面、组件说明
- **触发时机**：修改后台功能、添加新页面、调试后台问题
- **内容**：页面详解、组件说明、API 列表、路由管理

### 4. **项目整体上下文** (`.cursor/rules/project-context.mdc`)
- **用途**：项目整体架构、目录结构、核心模块说明
- **触发时机**：架构调整、添加新模块、需要理解项目全貌
- **内容**：目录结构、核心模块说明、数据流、技术栈

---

## 🔄 规则自动收集机制（元规则）

### 工作原理

**识别规则时机**：
- 用户明确提到"规则"、"不要"、"必须"、"禁止"等关键词
- 用户纠正 AI 的行为
- 用户表达对某种做法的偏好
- 用户说"记住"、"以后"、"下次"等表示未来行为的词

**判断是否收集**：
- ✅ 适用于整个项目的通用规则
- ✅ 会重复发生的行为规范
- ✅ AI 容易犯的错误或误解
- ❌ 一次性的具体任务指令
- ❌ 特定模块的业务逻辑（应记录在模块的 `.prompt.md` 中）

**规则分类与目标文件**：
- **API 接口生成** → `api-generation.mdc`
- **UI/UX 设计** → `ui-guidelines.mdc`
- **后台页面功能** → `backend-admin-context.mdc`
- **项目架构** → `project-context.mdc` + 本文件
- **通用规则** → 本文件

**创建新规则文件的条件**：
- 规则不属于现有任何文件
- 是独立的、重要的主题（如"测试规范"、"部署规范"）
- 新文件必须包含：`alwaysApply: false` 声明、文件用途说明、触发时机说明
- 在本文件的"规则文件索引"章节添加新文件的说明

**规则优化原则**：
- ✅ 不冗余：尽量在现有规则中完善，而不是创建新章节
- ✅ 精简关键信息：只保留关键信息让 AI 知道即可，避免冗长描述
- ✅ 优先合并：新规则优先合并到相关章节，而非单独列出

### 收集示例

**示例 1 - API 技术细节**：
- 用户说："model.ts 中的 LIMIT 必须直接拼接，不能用占位符"
- AI 识别：API 生成的技术细节
- AI 收集：添加到 `api-generation.mdc` 的"常见错误与修复"部分

**示例 2 - 架构变更**：
- 用户说："删除 template/ 目录，改为直接用规则生成代码"
- AI 识别：重大架构变更
- AI 收集：更新 `project-context.mdc` 记录架构变更
- **关键**：必须同步搜索并更新所有引用该目录的文档（详见"架构变更同步规则"）

---

## 🎯 基本规则

### 项目定位
- ✅ **AI 驱动的 Mock API 测试服务器**
- ✅ 用户通过 Cursor 提供接口文档/需求，AI 生成可运行的接口
- ✅ 生成的接口有真实的数据库支持，提供后台管理页面

### 核心工作流程
1. 用户提出需求（完整文档或简单需求如"生成订单管理"）
2. AI 根据 `api-generation.mdc` 规则生成模块代码
3. AI 自动运行 `init-db.ts` 初始化数据库
4. AI 自动运行 `verify.ts` 验证功能可用性
5. 用户在后台管理页面查看、测试、管理接口

### 关键约束
- ✅ 使用中文与用户沟通
- ✅ 接口生成到 `src/generated/[模块名]/`，每个模块必须包含 `.prompt.md` 文件
- ✅ 模块特殊调整记录在模块级 `.prompt.md`，通用规则更新到 `.cursor/rules/`
- ✅ 后台管理功能不能受影响（最高优先级）

---

## 📝 文档识别规则

### 当用户要求生成接口时
- ✅ 用户可能提供完整文档或只说一句话（如"生成订单管理"）
- ✅ 默认理解为要求生成 API 模块
- ✅ 不要询问"你给我文档是要做什么？"
- ✅ 直接分析需求并生成代码
- ✅ 开始前简单复述理解到的需求即可

### 生成流程
1. 用户提出需求
2. AI 读取 `.cursor/rules/api-generation.mdc` 获取详细规范
3. 按照规范生成所有必需文件
4. 自动运行 `init-db.ts` 初始化数据库
5. 自动运行 `verify.ts` 验证功能可用性
6. 向用户报告验证结果和可访问的管理页面地址

---

## 📄 文件管理规则

### 禁止生成的文件
- ❌ 总结类文档（如 PROJECT_SUMMARY.md、CHANGELOG.md）
- ❌ 无意义文档（如 FIX_REPORT.md、TODO.md）
- ❌ 项目总结、进度报告（在聊天中说明即可）
- ❌ 调试/测试用的临时脚本文件

### 允许生成的文件
- ✅ `README.md`（项目说明，必需）
- ✅ 模块级 `.prompt.md`（每个模块必需）
- ✅ API 模块的必需文件（见 `api-generation.mdc`）

### 临时文件管理
- ❌ 禁止保留调试/测试用的临时脚本文件
- ✅ 完成任务后必须清理所有临时生成的文件（.ts 和 .js）
- ✅ 永久性的初始化脚本应该放在 `scripts/` 目录
- ✅ 模块的初始化脚本应命名为 `init-db.ts`

---

## 🏗️ 架构变更同步规则

### 核心原则
当项目架构发生变更时，必须同步更新所有相关的文档、代码、规则文件。

### 触发条件
- 添加/删除核心目录（如 `src/template/`、`src/services/`）
- 修改项目结构（如合并目录、重命名核心模块）
- 改变代码生成方式（如从模板生成改为规则生成）
- 修改核心工作流程（如数据库初始化方式、路由注册方式）

### 执行流程

**1. 搜索所有引用**
使用 `grep` 或 `codebase_search` 搜索变更内容的所有引用

**2. 更新所有引用文件**
- ✅ 修改过时的目录结构说明
- ✅ 更新工作流程描述
- ✅ 修正代码示例和路径引用
- ✅ 同步更新使用说明页面（`public/admin/pages/help.js`）

**3. 更新规则文件**
- ✅ `ai-rules.mdc` - 更新文件结构章节
- ✅ `project-context.mdc` - 更新架构说明
- ✅ `api-generation.mdc` - 更新相关的生成规则（如有）
- ✅ 其他受影响的规则文件

**4. 验证一致性**
- ✅ 确认所有文档描述一致
- ✅ 确认代码可以正常运行

### 案例：删除 template/ 目录

**必须执行的操作**：
1. 删除 `src/template/` 目录
2. 搜索所有引用：`grep -r "template" README.md public/admin .cursor/rules`
3. 更新 `README.md` 中的项目结构
4. 更新 `public/admin/pages/help.js` 中的使用说明
5. 更新 `project-context.mdc` 记录架构变更

**自动收集触发**：
当用户指出"文档与实际不符"时，说明自动收集机制未生效，AI 应该：
1. 承认遗漏
2. 立即执行完整的同步更新
3. 反思为什么当时没有触发自动收集

---

## 🧪 需求分析规则

### 适用范围
**本规则仅适用于"项目本身的功能改进需求"，不适用于"根据接口文档生成 API 模块"的需求**

- ✅ 适用：改进管理后台、修改项目结构、优化功能、添加工具等
- ❌ 不适用：用户提供接口文档要求生成 API 模块（直接执行即可）

### 分析流程
- ✅ 当用户提出项目功能改进需求时，AI 应先分析合理性，提出改进建议
- ✅ 如有多种实现方案，应向用户说明各方案的优缺点
- ✅ 征得用户确认后再执行实施
- ✅ 如果用户方案存在明显问题，必须指出并建议替代方案

---

## 📌 重要提醒

### 1. 后台管理优先
- 生成的接口必须能在后台管理页面正常使用
- 如果后台功能异常，说明生成的代码有问题
- 详细说明见 `backend-admin-context.mdc`

### 2. 生成的模块可以修改
- 修改前必须评估影响：是否会影响后台管理功能
- 修改后必须验证：确保后台管理功能正常
- 特殊调整记录在模块的 `.prompt.md`，通用问题更新规则文件

### 3. 规则文件分工
- 本文件：通用规则、文件管理、工作流程
- `api-generation.mdc`：API 生成的详细技术规范
- `ui-guidelines.mdc`：前端页面的设计规范
- `backend-admin-context.mdc`：后台管理系统说明
- `project-context.mdc`：项目整体架构说明

### 4. 核心工具
- **BaseModel**（`src/core/BaseModel.ts`）：提供基础 CRUD 方法
- **Res**（`src/core/Response.ts`）：支持三种响应格式（B2B、ERP、Ebooking）
- **Faker**（`src/utils/faker.ts`）：根据表结构自动生成测试数据

---

**最后提醒**：
- 本文件是所有规则的入口和索引
- 具体技术细节在专项规则文件中
- 发现新的通用规则时，立即更新本文件
- 所有规则文件都有自动收集机制，发现新规则时，AI 应自动识别并更新对应文件
